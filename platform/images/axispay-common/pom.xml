<?xml version="1.0" encoding="UTF-8"?>
<!--
  ============================================================================
  axispay-common — shared runtime for every AxisPay microservice
  ============================================================================
  Every service depends on this module, which is why all sixteen expose an
  identical operational surface:

      /healthz    liveness   — is the process alive?
      /readyz     readiness  — can THIS instance serve traffic right now?
      /startupz   startup    — has initialisation finished?
      /metrics    Prometheus exposition
      /api/v1/_info          — version, pod, node  (makes load balancing visible)

  That uniformity is a teaching decision. Once a student understands one
  service, they understand all sixteen.
  ============================================================================
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.axispay</groupId>
        <artifactId>axispay-platform</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>axispay-common</artifactId>
    <name>AxisPay Common</name>
    <description>Shared operational runtime: probes, metrics, logging, errors, HTTP client, money.</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- A library module, not an application: no repackaging into a fat jar. -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
