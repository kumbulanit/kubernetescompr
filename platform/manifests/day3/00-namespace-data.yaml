apiVersion: v1
kind: Namespace
metadata:
  name: axispay-data
  labels:
    app.kubernetes.io/part-of: axispay
    axispay.io/zone: data
    axispay.io/pci-scope: "true"
    axispay.io/day-introduced: "3"
    kubernetes.io/metadata.name: axispay-data
  annotations:
    axispay.io/description: >-
      The vault. PostgreSQL, Redis and RabbitMQ. From Day 4 only axispay-core
      and axispay-async may reach it — never axispay-edge.
